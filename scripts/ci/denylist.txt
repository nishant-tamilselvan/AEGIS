# Patterns that must never appear in this repository (case-insensitive regular
# expressions, one per line). `python scripts/ci/repo_checks.py denylist` enforces them.
#
# Keep organization-specific names out of this public file. Put them in
# `.denylist.local` (gitignored) or the AEGIS_DENYLIST environment variable
# (patterns separated by ";") instead.

# Credentials
Authorization:\s*Basic\s+[A-Za-z0-9+/=]{16,}
Bearer\s+[A-Za-z0-9._~+/-]{24,}=*
-----BEGIN [A-Z ]*PRIVATE KEY-----
(?:ghp|gho|ghs|ghu|github_pat)_[A-Za-z0-9_]{20,}
AKIA[0-9A-Z]{16}
xox[baprs]-[A-Za-z0-9-]{10,}
sk-ant-[A-Za-z0-9_-]{20,}

# Internal hostnames
https?://[^\s/"')]*\.(?:internal|intranet|corp|lan)(?:[/:"')\s]|$)
